import "./globals.css";

export const metadata = {
  title: "Ideas from: Smart Contract Auditor AI",
  description: "Revolutionary AI solution based on Ideas from: Smart Contract Auditor AI",
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}